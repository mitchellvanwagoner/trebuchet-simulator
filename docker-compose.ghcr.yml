services:
  trebuchet-web:
    image: ghcr.io/mitchellvanwagoner/trebuchet-simulator:latest
    pull_policy: always
    ports:
      - "8501:8501"
    volumes:
      - ./outputs:/app/outputs
      - /mnt/user/appdata/treb-sim:/app/data
