services:
  trebuchet-web:
    image: ghcr.io/mitchellvanwagoner/trebuchet-simulator:latest
    pull_policy: always
    ports:
      - "8501:8501"
    volumes:
      - ./outputs:/app/outputs
      - user_defaults:/app/user_defaults.json

volumes:
  user_defaults:
